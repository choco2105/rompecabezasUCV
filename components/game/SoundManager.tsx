'use client';

/**
 * Este componente queda como placeholder.
 * La inicialización del audio se hace desde el botón "Empezar"
 * en la pantalla principal (app/page.tsx), que respeta la
 * política anti-autoplay de los navegadores.
 */
export default function SoundManager() {
  return null;
}